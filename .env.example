# Database
DB_PASSWORD=password

# JWT
SECRET_KEY=change-me-in-production

# Default admin credentials (created on first startup)
ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_PASSWORD=changeme

# Payment adapter: stub | cash (real adapters added later)
PAYMENT_ADAPTER=stub

# Pool name displayed on kiosk
POOL_NAME=My Pool
