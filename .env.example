# Database
DB_PASSWORD=change-me-strong-password

# JWT secret (use a random 64+ character string)
SECRET_KEY=change-me-in-production

# Default admin credentials (created on first startup only)
ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_PASSWORD=changeme

# Pool name displayed on kiosk idle screen
POOL_NAME=My Pool
